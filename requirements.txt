numpy>=1.24
pandas>=2.0
opencv-python>=4.8
matplotlib>=3.7
streamlit>=1.32
# DeepLabCut and torch are intentionally pinned loosely due to platform/GPU variance.
# Install a compatible torch build first if needed, then:
deeplabcut>=2.3
